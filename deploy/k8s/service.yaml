apiVersion: v1
kind: Service
metadata:
  name: autonomous-agent-service
  namespace: agent-platform
  labels:
    app.kubernetes.io/name: autonomous-agent-engine
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http-api
  selector:
    app.kubernetes.io/name: autonomous-agent-engine
